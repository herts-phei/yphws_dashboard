
# UI ----------------------------------------------------------------------

question_mod <- function(id) {
  
  ns <- NS(id)
  
}


# Server ------------------------------------------------------------------

question_mod_server <- function(id,
                                q_coded,
                                chk_var,
                                chk_stats,
                                comp) {
  moduleServer(
    id, 
    function(input, output, session) {
      
      
      }
  )
  }